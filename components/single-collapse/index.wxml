<view>
    <van-collapse value="{{ activeNames }}" bind:change="onChange">
        <van-collapse-item title="{{title}}" name="1">
            <slot></slot>
        </van-collapse-item>
    </van-collapse>
</view>
